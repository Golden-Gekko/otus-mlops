apiVersion: v1
kind: Service
metadata:
  name: fraud-api-service
  namespace: fraud-api
  labels:
    app: fraud-api
spec:
  type: NodePort
  selector:
    app: fraud-api
  ports:
    - protocol: TCP
      port: 30080
      targetPort: 8000
      nodePort: 30080
      name: http
